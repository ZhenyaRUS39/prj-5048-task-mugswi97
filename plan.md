# Plan: prj-5048-task-mugswi97

**Project:** `PRJ-1805`  
**Task ID:** `task-mugswi97`  
**Repo:** `prj-5048-task-mugswi97`  

## Summary

Создать репозиторий prj-5048-task-mugswi97 и реализовать минимальный async webhook endpoint (POST принимает задачу, отвечает 202, обрабатывает в фоне) с базовыми unit/integration тестами.

## Subtasks

### 1. Подготовить репозиторий и зависимости

- **ID:** `sub-1`
- **Profile:** `20razrab1`
- **Tester:** `TBD`
- **Status:** `pending`
- **Description:** Создать локально и в GitHub репозиторий prj-5048-task-mugswi97 (если отсутствует), инициализировать структуру: src/, tests/, requirements.txt (или pyproject.toml), README с описанием endpoint'а. Зафиксировать FastAPI + uvicorn + httpx (для тестов) как зависимости.
- **Test plan:** gh repo view ZhenyaRUS39/prj-5048-task-mugswi97 возвращает 200; репозиторий клонируется; pip install -r requirements.txt завершается без ошибок; python -c 'import fastapi, uvicorn, httpx' печатает версии.

### 2. Реализовать async webhook handler

- **ID:** `sub-2`
- **Profile:** `30razrab2`
- **Tester:** `TBD`
- **Status:** `pending`
- **Description:** Реализовать FastAPI-приложение с POST /webhook, который принимает JSON {task_id, payload}, валидирует схему (pydantic), немедленно возвращает 202 Accepted с task_id, а обработку (sleep + запись в in-memory dict статусов tasks[task_id]=done) выполняет через asyncio.create_task. Добавить GET /tasks/{task_id} для проверки статуса.
- **Test plan:** uvicorn src.main:app стартует; curl -X POST /webhook с валидным JSON возвращает 202 и task_id в теле; в течение ~1 сек GET /tasks/{task_id} возвращает {status: done}; невалидный JSON даёт 422.
- **Dependencies:** `sub-1`

### 3. Покрыть handler тестами (pytest + httpx AsyncClient)

- **ID:** `sub-3`
- **Profile:** `20razrab1`
- **Tester:** `TBD`
- **Status:** `pending`
- **Description:** Написать тесты в tests/: test_webhook_accepts_and_returns_202, test_task_processed_in_background, test_get_task_status, test_invalid_payload_returns_422. Использовать httpx.AsyncClient + ASGITransport (без живого uvicorn).
- **Test plan:** pytest -v проходит все 4 теста зелёным; покрытие строки /webhook и /tasks/{task_id} >= 90%; pytest -q --tb=short не показывает warnings уровня error.
- **Dependencies:** `sub-2`

### 4. CI smoke-проверка и финальный handoff

- **ID:** `sub-4`
- **Profile:** `30razrab2`
- **Tester:** `TBD`
- **Status:** `pending`
- **Description:** Добавить минимальный GitHub Actions workflow (.github/workflows/test.yml) на push/PR, запускающий pip install -r requirements.txt && pytest -q. Убедиться, что main-ветка зелёная. Вернуть summary Айтишнику (ссылка на PR/commit, вывод pytest, пример curl-вызова).
- **Test plan:** gh run list --limit 1 показывает success; git log origin/main --oneline -n 5 содержит коммит с тестами; README содержит рабочий пример curl.
- **Dependencies:** `sub-3`


---
*Generated by Hermes Orchestrator*
